branch-defaults:
  main:
    environment: schoolback-dev
global:
  application_name: schoolback
  default_platform: Corretto 21 AL2023 version 4.6.5
  default_region: eu-west-3
  workspace_type: Application
